{
  // 指定哪些ts文件需要编译
  "include": [
    "./src/**/*" // ** 任意文件夹，* 任意文件
  ],
  // 表示不包含的目录
  "exclude": [],
  "compilerOptions": {
    // 用来指定 ts 被编译为的ES的版本
    // 'es3', 'es5', 'es6', 'es2015', 'es2016', 'es2017', 'es2018', 'es2019', 'es2020', 'es2021', 'esnext'
    "target": "ES2015",
    // 指定要使用的的模块化的规范
    // 默认值:'none', 'commonjs', 'amd', 'system', 'umd', 'es6', 'es2015', 'es2020', 'es2022', 'esnext', 'node12', 'nodenext'
    "module": "es6",
    // 指定项目中要使用到的库。默认注释，不实用
    // "lib": [] // 可用值可以通过写一个错误的来查看。
    // 指定编译后文件所在的目录
    "outDir": "./dist",
    // 将代码合并为一个文件
    // 将全局作用域的文件合并为同一个文件。只能是amd或system可以用
    // "outFile": "./dist/app.js"
    // 是否对js文件进行编译。默认是false
    "allowJs": false,
    // 是否检查JS代码是否符合规范。默认false
    "checkJs": false,
    // 是否移除注释
    "removeComments": false,
    // 不生成编译后的文件
    "noEmit": false,
    // 当有错误时，不编译成文件
    "noEmitOnError": true,
    // ================
     // 所有严格检查的总开关，默认建议开启
     "strict": true,
    // 编译后的文件是否是严格模式.默认false
    "alwaysStrict": false,
    // 不允许函数中的变量隐式推导为any。默认false
    "noImplicitAny": true,
    // 不允许不明确类型的this
    "noImplicitThis": true,
    // 严格的检查空值
    "strictNullChecks": true,
  }
}
